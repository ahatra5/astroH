// let slideAtual = 0;
// const totalSlides = 4;
// const carousel = document.querySelector('.carousel-inner');
// const images = document.querySelectorAll('.carousel-img');

// function reiniciar() {
//     const translateValue = -slideAtual * 100 + '%';
//     carousel.style.transform = 'translateX(' + translateValue + ')';
// }


// function changeSlide(direction) {
//     // Atualiza o slide atual baseado na direção fornecida
//     slideAtual += direction;

//     // Verifica se o slide atual ultrapassou os limites
//     if (slideAtual < 0) {
//         slideAtual = totalSlides - 1; // Define para o último slide
//     } else if (slideAtual >= totalSlides) {
//         slideAtual = 0; // Define para o primeiro slide
//     }

//     reiniciar();
// }

